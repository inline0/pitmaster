# Benchmark Reports

Committed benchmark baselines live here.

- `baseline.json` is the current local canonical report captured with `./bin/bench-baseline`
- `smoke-thresholds.json` defines the committed smoke-suite ceiling used by CI
- machine metadata is embedded in the JSON output
- ad-hoc local comparisons should use `*.local.json`, which is ignored by Git
